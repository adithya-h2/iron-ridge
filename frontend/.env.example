# Point this at your running FastAPI backend once endpoints are confirmed
VITE_API_BASE_URL=http://localhost:8000

# true = use built-in mock data (works with zero backend running)
# false = call the real backend at VITE_API_BASE_URL
VITE_USE_MOCKS=true
